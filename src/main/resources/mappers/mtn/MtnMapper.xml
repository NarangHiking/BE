<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.naranghiking.mtn.dao.MtnDao">

    <!--  산 전체 조회 -->
    <select id="selectAll" resultType="com.naranghiking.mtn.dto.Mtn">
        select * from mountains
    </select>

    <!-- 산 이름으로 조회 -->
    <select id="selectByName">
        select * from mountains
        where name like CONCAT('%', #{name}, '%')
    </select>
    <!--  산 단일 조회  -->
    <select id="select" resultType="com.naranghiking.mtn.dto.Mtn">
        select * from mountains
        where id = #{id}
    </select>
    <!-- 산 정보 삽입 -->
    <insert id="insert">
        insert into mountains (name, location, height, description, original_filename, stored_filename)
        values (#{name}, #{location}, #{height}, #{description}, #{originalFilename}, #{storedFilename})
    </insert>

    <update id="update">
        update mountains
        set name = #{name},
            location = #{location},
            height = #{height},
            description = #{description}
        where id = #{id}
    </update>

    <delete id="delete">
        delete from mountains
        where id = #{id}
    </delete>
</mapper>